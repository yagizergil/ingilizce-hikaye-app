/**
 * DEPRECATED PATH — kept only so `@/theme/colors` (used by `useTheme.ts`
 * and any external imports) keeps resolving. The actual token values now
 * live in `src/theme/tokens/colors.ts`, rebuilt from the approved mockups
 * in `mockups/*.html` (see that file's header comment for the full
 * rationale, including the reader.html ink-color discrepancy and the
 * sepia/dark derivation notes). Do not add values here — edit
 * `tokens/colors.ts` instead.
 */
export * from "@/theme/tokens/colors";
