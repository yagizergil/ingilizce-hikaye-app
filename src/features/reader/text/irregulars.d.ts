/**
 * Type declarations for the plain-CommonJS `irregulars.js` sitting next to
 * this file. See `tokenizer.d.ts` for why this pairing pattern is used
 * instead of converting the JS source itself to TypeScript.
 */

export const irregularLemmas: Record<string, string>;
export const abbreviations: string[];
